// Shared by the server schema and the browser: keep free of dependencies.
export const MIN_QUESTION_LENGTH = 3;
export const MAX_QUESTION_LENGTH = 1500;
/** Previous questions sent with a follow-up. */
export const MAX_HISTORY = 4;
